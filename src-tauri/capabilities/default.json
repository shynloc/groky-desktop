{
  "$schema": "../gen/schemas/capability-schema.json",
  "identifier": "default",
  "description": "Capability for Groky app",
  "local": true,
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "shell:allow-open",
    "shell:allow-spawn",
    "shell:allow-kill",
    "shell:default",
    "dialog:allow-open"
  ]
}
